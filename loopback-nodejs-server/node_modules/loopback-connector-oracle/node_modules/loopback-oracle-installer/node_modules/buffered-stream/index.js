
var FixedBufferedStream = require('./lib/FixedBufferedStream');
var GentleBufferedStream = require('./lib/StackBufferedStream');

module.exports = function(size){
  return new FixedBufferedStream(size);
}
module.exports.fixed = function(size){
  new FixedBufferedStream(size);
}
module.exports.stack = function(size){
  new StackBufferedStream(size);
}
